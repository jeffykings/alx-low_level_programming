/**
  * _strlen - function that returns the length of a string
  * @s: pointer that points to a char variable
  * Return: the length of a string
  */

int _strlen(char *s)
{
	int i;

	for (i = 0; s[i] != '\0'; i++)
	{
	}

	return (i);
}
